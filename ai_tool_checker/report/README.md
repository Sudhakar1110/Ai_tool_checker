# Report README

The **report** folder contains custom script reports used by the app.

- `ai_tool_industry_summary` – Shows tool count, average rating, and usage per industry.
- `ai_tool_usage_report` – Time‑series report of tool check logs.

Each report has a JSON definition and a Python script that builds the data.
