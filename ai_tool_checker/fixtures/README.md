# Fixtures README

The **fixtures** folder holds data fixtures that are automatically installed with the app.

- `install.py` – Contains functions that run after installation/migration to load default roles, permissions, and sample data.
- JSON fixtures (e.g., roles, doc types) are exported here for easy deployment across environments.
