# Notification README

This folder contains notification JSON definitions used by the app.

- `ai_tool_new_tool_added.json` – Notifies AI Tool Managers when a new tool is created.
- `ai_tool_review_submitted.json` – Notifies the tool owner when a review is submitted.

These are loaded via the `fixtures` list in `hooks.py`.
