# Page README

The **page** folder contains custom Frappe web pages.

- `ai_tool_dashboard` – The main dashboard page that displays stats, top tools, and recent activity.
- Each page has a JSON definition and a JS file for client‑side behaviour.
